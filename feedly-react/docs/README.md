# Feedly React – Docs

- **CONVERSION-PROCESS.md** – Step-by-step process for turning a SingleFile HTML page into real React components (what we did for the homepage).
- **REMAINING-PAGES-PLAN.md** – Order and workflow for converting the rest of the saved pages one by one to the same standard.

Use these together when converting each remaining file so every page follows the same process and stays consistent.
